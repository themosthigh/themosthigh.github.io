---
import { twJoin, twMerge } from "tailwind-merge";
const { href, label } = Astro.props;
const currentPath = Astro.url.pathname;

const active = _ => currentPath === href;
---

<a
	href={href}
	class={twMerge("text-gray-500", active(href) ? "text-white" : "")}
>
	{label}</a
>
